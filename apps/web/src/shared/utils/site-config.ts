/**
 * Public contact channels for the SwiftTab website.
 *
 * This is the single source of truth for the contact links shown in the
 * header, footer, and final CTA on the homepage.
 *
 * Launch blocker: no legitimate contact details existed in the repository
 * when these were created, so both values are intentionally empty. Before
 * public launch, replace the empty strings with the real business support
 * email and/or WhatsApp number.
 *
 * While both values are empty, contact links are hidden — no fake or broken
 * contact path is ever rendered to visitors. As soon as a real value is set
 * here, the header, footer, and CTA contact links appear automatically.
 */
export const SUPPORT_EMAIL: string = ""; // e.g. "support@swifttab.com"
export const SUPPORT_WHATSAPP: string = ""; // e.g. "+91 90000 00000" or "919000000000"
